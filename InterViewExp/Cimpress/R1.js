/*
   DSA -
    1. Check if number is even or Odd without any conditional Statement.
    2. Longest Substring Without Repeating Characters (Sliding Window).

*/

// console.log('23' + '10' + '3' + 2 * 5);
// console.log('23' + '10' + '3' + 2 - 5);

// call(), apply(), bind(), promise.All(), promise.allSettled()

/*
Declare and arrow function in TypeScript , I missed the function return type
const myFunction = (param1: string, param2: number): boolean => {
  return true;
};

*/
